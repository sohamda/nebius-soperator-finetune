# Core ML
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
datasets>=2.18.0

# LoRA / PEFT
peft>=0.10.0
trl>=0.8.0

# Quantization (optional, for QLoRA)
bitsandbytes>=0.43.0

# Flash Attention 2 - RECOMMENDED for maximum GPU utilization
# Provides 2-4x speedup and better GPU compute usage
# Install with: pip install flash-attn --no-build-isolation
flash-attn>=2.5.0

# Utilities
numpy
sentencepiece>=0.2.0
protobuf>=4.25.0
scipy>=1.12.0
